name = "lottery-betting-system"
compatibility_date = "2026-01-01"

# Database bindings and other sensitive configs are skipped for security.
# Please bind your D1 database (Variable name: DB) via the Cloudflare Pages Dashboard.
